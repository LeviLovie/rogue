#include <iostream>
using namespace std;

namespace snl {
    void PatchFile(string);
    int CheckFile(string path);
    int CheckLogFile(string path);
    void CheckPatchFile(string path);
    void CheckPatchLogFile(string path);
}